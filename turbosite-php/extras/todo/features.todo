Sorted by priority:

- Implement the errors to mail feature

- Prevent url access to folders like storage, _dev, _trash, etc.. and allow to folders like storage/custom and similar.
  Review that all this is correctly implemented